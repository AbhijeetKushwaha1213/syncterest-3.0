
import SettingsLayout from "@/components/settings/SettingsLayout";

const SettingsIndexPage = () => {
  // Just show the settings layout with account as default content
  return <SettingsLayout />;
};

export default SettingsIndexPage;
