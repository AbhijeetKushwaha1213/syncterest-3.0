
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const TrendingChannels = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Feature Removed</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">
          Trending channels feature has been removed from the application.
        </p>
      </CardContent>
    </Card>
  );
};

export default TrendingChannels;
